# Build
/build/
/out/
**/build/
.gradle/

# IntelliJ / Eclipse
.idea/
*.iml
*.ipr
*.iws
.classpath
.project
.settings/
bin/

# Kotlin / Gradle
.kotlin/

# Secrets / local config — NEVER commit keys
.env
.env.*
*.local.properties
application-secret.yml
application-secret.properties
src/main/resources/secrets/

# Python (deploy scripts) cache
__pycache__/
*.pyc

# Logs
*.log
/logs/

# OS
.DS_Store
Thumbs.db

# Local runtime DB (H2 dev) — never commit
/data/
*.mv.db
*.trace.db

# 빌드된 SPA 정적 산출물(Docker/로컬 단일 컨테이너 검증 시 생성) — 소스 아님
/src/main/resources/static/

# Claude Code local config / hooks
.claude/

# 카드 렌더러(satori) - node_modules 는 컨테이너에서 재설치, 로컬 테스트 아티팩트 제외
/render/node_modules/
/render/out.png
/render/out.b64
/render/sample.json
/render/render-err.txt
